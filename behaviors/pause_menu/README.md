# PauseMenu — Sistema de Pausa

Pausa/retoma a árvore via `get_tree().paused`. Detecta `pause_action` (padrão
`ui_cancel`). `toggle()`, `pause()`, `resume()`, `is_paused()`. Emite `paused`
e `resumed`. Sem dependências.
