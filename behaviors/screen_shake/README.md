# ScreenShake — Tremor de Tela

Aplica offset aleatório na `Camera2D` para efeito de tremor. `trigger()` dispara.
Parâmetros: `intensity`, `duration`, `decay`. Emite `shake_started`/`shake_ended`.
Sem dependências.
