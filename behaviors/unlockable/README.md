# Unlockable — Metaprogression

Desbloqueia conteúdo baseado em condições (`achievement`, `level`, `currency`).
Auto-detecta Achievement, XPLevel e Currency irmãos. `is_unlocked(id)`,
`_check_all_unlocks()`. Emite `unlocked`. Sem dependências.
