# Arquivos de ambiente e credenciais (nunca commitar — chaves de API)
.env
.env.*
credentials.*
*.key
*.pem

# Arquivos temporários do MCP (nunca commitar)
*_mcp_incr_compile.gd
*.tscn.tmp
*.json.tmp
*.py.tmp

# Configuração local (cada máquina tem a sua — use config.json.example como modelo)
config.json
config.local.json

# Python
__pycache__/
*.pyc
*.egg-info/

# Godot
.godot/
*.import
*.uid

# Ambiente virtual
.venv/
.venv_quebrado/

# Build artifacts
builds/
export/

# Cache
art_cache/
classdb_cache/
temp_art/
workflow_logs/

# IDE
.vscode/
.mcp_proof/

# config.json removido do gitignore — versionado para setup multiplataforma
# .roadmap_progress.json removido do gitignore — versionado para setup multiplataforma
ws_bridge.py
