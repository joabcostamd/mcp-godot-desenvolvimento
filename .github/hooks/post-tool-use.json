{
  "hooks": {
    "PostToolUse": [
      {
        "type": "command",
        "command": "python",
        "args": ["-c", "print('[HOOK] Tool executada. Se foi edit, verificar consistencia.')"]
      }
    ],
    "Stop": [
      {
        "type": "command",
        "command": "python",
        "args": ["-c", "import sys; sys.path.insert(0, '.'); from tools.registry_validation import validate_tool_registry_consistency; r = validate_tool_registry_consistency(); ok = r['counts']['tools_sem_handler'] == 0; print(f'[AUTO-AUDIT] tools_sem_handler={r[\"counts\"][\"tools_sem_handler\"]} -> {\"PASS\" if ok else \"FAIL - CORRIJA ANTES DE FINALIZAR\"}')"]
      }
    ]
  }
}
